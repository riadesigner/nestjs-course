import { AgeValidationPipe } from './age-validation-pipe.pipe';

describe('AgeValidationPipePipe', () => {
  it('should be defined', () => {
    expect(new AgeValidationPipe()).toBeDefined();
  });
});
