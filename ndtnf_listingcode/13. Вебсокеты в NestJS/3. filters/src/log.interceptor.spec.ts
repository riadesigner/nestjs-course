import { Log.InterceptorInterceptor } from './log.interceptor.interceptor';

describe('Log.InterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new Log.InterceptorInterceptor()).toBeDefined();
  });
});
