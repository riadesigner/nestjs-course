import { App.Age.ValidationPipe } from './app.age.validation.pipe';

describe('App.Age.ValidationPipe', () => {
  it('should be defined', () => {
    expect(new App.Age.ValidationPipe()).toBeDefined();
  });
});
